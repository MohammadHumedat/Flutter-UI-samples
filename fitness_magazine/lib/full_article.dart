import 'package:flutter/material.dart';

class FullArticle extends StatelessWidget {
  const FullArticle({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Stack(
        
      ),
    );
  }
}